let resolvers = {};

module.exports = resolvers;


<% for (let i=0; i < models.length; i++ ){ -%>
  var resolvers<%= models[i][0] -%> = require('./<%= models[i][0].toLowerCase()-%>');
<%}-%>

<% for (let i=0; i < models.length; i++ ){ -%>
  for( resolver in resolvers<%= models[i][0]-%> )
  {
    resolvers[resolver] = resolvers<%= models[i][0] -%>[resolver];
  }
<%}-%>
