
const {connection} = require('../connection');

const <%- name -%> = connection.define('<%- nameLc -%>',{
  <% for (var key in attributes) { %>
    <%= key %>: {
        type: Sequelize.<%= attributes[key].toUpperCase() %>
    },
  <% } %>
});

module.exports.<%- nameLc -%> = <%- name -%>;
