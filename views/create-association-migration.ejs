'use strict';

module.exports = {

    up: function(queryInterface, Sequelize) {
        return queryInterface.addColumn('<%= source %>', '<%= key %>',{
               type: Sequelize.INTEGER,
               references: {
                 model: '<%=get_name.toLowerCase() %>',
                 key: 'id'
               }

        });
    },

    down: function(queryInterface, Sequelize) {
      return queryInterface.removeColumn('<%= source %>', '<%= key %>');
    }

};
