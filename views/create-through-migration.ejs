'use strict';

module.exports = {

  up: function(queryInterface, Sequelize){
    return queryInterface.createTable('<%-through-%>', {

      createdAt: {
        type: Sequelize.DATE
      },

      updatedAt: {
        type: Sequelize.DATE
      },

      <%- sourceKey-%>:{
        type: Sequelize.INTEGER,
        references:{
          model: '<%-source-%>',
          key: 'id'
        }
      },

      <%- targetKey-%>:{
        type: Sequelize.INTEGER,
        references:{
          model: '<%-resolver-%>',
          key: 'id'
        }
      }
    });
  },

  down: function(queryInterface, Sequelize){
    return queryInterface.dropTable('<%-through-%>');
  }

};
